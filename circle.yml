machine:
  environment:
    PROJECT: $CIRCLE_PROJECT_REPONAME
    BRANCH: $CIRCLE_BRANCH
    COMMIT: $CIRCLE_SHA1
  node:
    version: 4.2.2

dependencies:
  override:
    - echo "testing"

test:
  override:
    - echo "no tests"

deployment:
  staging:
    branch: /^(?!nodeploy-).*$/
    commands:
      - ./artifact.sh