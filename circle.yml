machine:
  node:
    version: 4.2.2

dependencies:
  pre:
    - sudo apt-get install gdal-bin
    - gem install bundler && bundle install

deployment:
  staging:
    branch: master
    commands:
      - npm run-script dist
      - npm run-script devdeploy