{
  "publicRoot": "dist",
  "build": {
    "commands": ["apt-get update && apt-get -y install gdal-bin && npm install", "NODE_ENV=production MINARD=true npm run build"],
    "cache": {
      "key": "%CI_PROJECT_PATH%",
      "paths": ["node_modules/"]
    }
  }
}
