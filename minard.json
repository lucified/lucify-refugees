{
  "publicRoot": "dist/embed/the-flow-towards-europe-updating",
  "build": {
    "commands": ["apt-get update && apt-get -y install gdal-bin  && npm install", "NODE_ENV=production npm run build"],
    "cache": {
      "key": "%CI_PROJECT_PATH%",
      "paths": ["node_modules/"]
    }
  }
}
